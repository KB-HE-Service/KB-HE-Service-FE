import { AxiosResponse } from "axios";
import { useNavigate } from "react-router-dom";

import { API, storeUserInfo, removeUserInfo, getUserInfo } from "@/shared";
import { PAGE_URL } from "@/shared/configs/path";

export const AuthService = () => {};
